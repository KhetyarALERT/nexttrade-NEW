# Use a workspace-local npm cache to avoid lock contention in CI environments
# (some hosted builders share ~/.npm across parallel jobs)
cache=.npm-cache

# Keep CI installs quieter/faster and avoid network-only optional steps
fund=false
audit=false
update-notifier=false
